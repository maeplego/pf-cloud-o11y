apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    uid: tempo
    access: proxy
    url: http://tempo.platform.svc.cluster.local:3200
    isDefault: true
    jsonData:
      nodeGraph:
        enabled: true
